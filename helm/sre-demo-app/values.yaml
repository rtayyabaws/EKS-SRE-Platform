replicaCount: 2

image:
  repository: 595552412690.dkr.ecr.eu-west-2.amazonaws.com/sre-demo-app
  tag: f420f5e
  pullPolicy: IfNotPresent

serviceAccount:
  create: true
  name: sre-demo-app

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

ingress:
  enabled: true
  className: nginx
  host: eks.nourdemo.com
  tls:
    enabled: true
    secretName: sre-demo-app-tls
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: eks.nourdemo.com

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

probes:
  liveness:
    path: /healthz
    initialDelaySeconds: 10
    periodSeconds: 10
  readiness:
    path: /readyz
    initialDelaySeconds: 5
    periodSeconds: 5

hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 60

pdb:
  enabled: true
  minAvailable: 1

serviceMonitor:
  enabled: true
  path: /metrics
  interval: 15s

networkPolicy:
  enabled: true